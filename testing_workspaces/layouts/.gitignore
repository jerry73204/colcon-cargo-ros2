build/
install/
log/
Cargo.lock

# Generated by colcon build; carries machine-specific absolute paths.
# preset_config's is copied from its committed preset.config.toml template.
src/**/.cargo/

# The target-dir preset_config asks for in its own config.
src/preset_config/cargo-target-of-my-own/
